name: Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.OWNER }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ secrets.OWNER }}/${{ secrets.IMAGE_NAME }}:latest

      - name: Copy deploy manifests to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.AZURE_HOST }}
          username: ${{ secrets.AZURE_USER }}
          key: ${{ secrets.AZURE_SSH_KEY }}
          source: ".deploy/compose.yml,.deploy/certs/tls.yml"
          target: "/home/${{ secrets.AZURE_USER }}/app"
          strip_components: 1 # drop the leading .deploy folder so compose.yml lands in target root

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.AZURE_HOST }}
          username: ${{ secrets.AZURE_USER }}
          key: ${{ secrets.AZURE_SSH_KEY }}
          script: |
            set -e

            mkdir -p /home/${{ secrets.AZURE_USER }}/app
            cd /home/${{ secrets.AZURE_USER }}/app

            # Write/update .env for compose (Traefik + image vars)
            cat > .env << 'EOF'
            OWNER=${{ secrets.OWNER }}
            IMAGE_NAME=${{ secrets.IMAGE_NAME }}
            DOMAIN=${{ secrets.DOMAIN }}
            EOF

            # Login to GHCR so the VM can docker pull
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ secrets.OWNER }}" --password-stdin

            docker compose -f compose.yml pull
            docker compose -f compose.yml up -d
            docker image prune -f
